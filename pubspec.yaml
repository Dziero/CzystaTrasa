name: hackathon_rower
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.9.0
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  flutter_dotenv: ^5.2.1
  geocoding: ^3.0.0
  permission_handler: ^11.3.1
  location: ^7.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
